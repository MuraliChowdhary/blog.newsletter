name: Deploy Frontend to Vercel

on:
  push:
    branches:
      - main
      - staging
      - production
  pull_request:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup pnpm FIRST
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      # 3️⃣ Setup Node.js (this will detect pnpm from previous step)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'pnpm'

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # 5️⃣ Build your frontend
      - name: Build project
        run: pnpm run build

      # 6️⃣ Deploy to Vercel
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          working-directory: ./
          alias-domains: |
            ${{ github.ref_name == 'staging' && 'staging.blog.nextdevs.me' || '' }}
            ${{ github.ref_name == 'production' && 'blog.nextdevs.me' || '' }}
        env:
          VERCEL_ENV: ${{ github.ref_name }}